{
  "name": "novel-tool",
  "private": true,
  "version": "3.0.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "setup": "node scripts/cli.mjs setup",
    "dev": "node scripts/cli.mjs dev",
    "build": "node scripts/cli.mjs build",
    "start": "node scripts/cli.mjs start",
    "check": "node scripts/cli.mjs check",
    "test": "node scripts/cli.mjs test",
    "format": "node scripts/cli.mjs format",
    "clean": "node scripts/cli.mjs clean"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^26.1.1",
    "prettier": "^3.9.5",
    "tsx": "^4.19.1",
    "typescript": "^5.5.4"
  },
  "packageManager": "npm@10.9.2",
  "engines": {
    "node": ">=22.12.0",
    "npm": ">=10.0.0"
  }
}
