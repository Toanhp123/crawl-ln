# Phase 4.2 Reader Controls Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add complete reader controls, live reading progress, swipe navigation, chapter selection context, brightness dimming, and adjacent chapter prefetching.

**Architecture:** Reader preferences remain local and theme-scoped. Scroll progress and swipe detection live in focused read-chapter model hooks, while ReaderPage composes them with existing toolbar, progress, navigation, and sheets. Chapter list highlighting is an optional entity UI capability.

**Tech Stack:** React, TypeScript, React Router, TanStack Query, Tailwind CSS, localStorage.

## Global Constraints

- Keep API contracts unchanged.
- Preserve FSD dependency direction.
- Do not add a UI dependency.
- Keep paragraph-anchor restoration as the source of truth for reading continuity.
- Verify with `npm run verify`.

---

### Task 1: Reader preference controls
- [x] Add brightness persistence and a reader-only dim overlay.
- [x] Expose theme, family, size, line height, paragraph spacing, width, alignment, weight, and reading toggles in the bottom sheet.

### Task 2: Live progress and navigation
- [x] Calculate chapter scroll progress and combine it with chapter position.
- [x] Add swipe and keyboard chapter navigation with interaction guards.
- [x] Prefetch both adjacent fetched chapters.

### Task 3: Chapter list context
- [x] Highlight the current chapter in the chapter sheet.
- [x] Keep search and pagination behavior unchanged.

### Task 4: Verification and packaging
- [x] Run complete repository verification.
- [x] Create a clean ZIP without node_modules.
- [x] Verify ZIP integrity.
