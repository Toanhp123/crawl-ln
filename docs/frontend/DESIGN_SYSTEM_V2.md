# Design System 2.0

Novel Tool uses primitive, semantic, and component token layers. Shared components consume semantic tokens; pages compose shared primitives and must not hard-code visual colors. Mobile controls use a 48px minimum target, safe-area-aware navigation, reduced-motion fallbacks, and separate regular/Reader shells.
