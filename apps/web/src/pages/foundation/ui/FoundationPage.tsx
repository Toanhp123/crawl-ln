import { API_BASE_URL } from '@/shared/config';

export function FoundationPage() {
  return (
    <main>
      <h1>Novel Tool</h1>
      <p>Frontend foundation is connected to {API_BASE_URL}.</p>
    </main>
  );
}
